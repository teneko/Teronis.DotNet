<?xml version="1.0" encoding="utf-8"?>
<!-- 
  This config file serves only the purpose being able to 
  select a source when passing -Source to nuget command.
-->
<configuration>
  <disabledPackageSources>
    <clear />
  </disabledPackageSources>
</configuration>

<!--
DependencyPublish.NetCoreApp.Identity.Bearer.csproj

This project is a synthetic project. It creates the filesystem
publishment for the project specified by PackageSourceReference.

We want to use the .props and .targets inside the project like
it would be when you installed the nuget packge, otherwise we
would have to separate path logic for local use in projects.
-->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netcoreapp3.1;net5.0</TargetFrameworks>
    <PackSourceAs>DependencyPublish</PackSourceAs>
    <PackageSourceReference>..\..\src\Teronis.AspNetCore.Identity.Bearer.csproj</PackageSourceReference>
  </PropertyGroup>
</Project>

namespace Teronis
{
    /// <summary>
    /// This interface has no purpose. It just serves
    /// as helper interface being used as constraint.
    /// </summary>
    public interface ISingleton
    { }
}

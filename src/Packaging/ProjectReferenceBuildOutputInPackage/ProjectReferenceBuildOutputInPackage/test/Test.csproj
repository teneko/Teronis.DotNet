<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <DisableGitVersionTaskAutoInstall>true</DisableGitVersionTaskAutoInstall>
    <DisableBuildOutputInPackageProjectsImports>true</DisableBuildOutputInPackageProjectsImports>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Resources.Designer.cs" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Remove="Resources.en-US.resx" />
    <EmbeddedResource Remove="Resources.resx" />
  </ItemGroup>

  <PropertyGroup>
    <!--<_SubOutDir Condition="'$(Platform)' == 'AnyCPU'">$(MSBuildProjectDirectory)\bin\  $(Platform)</_SubOutDir>-->
    <!--<TeronisPackagingProjectReferenceBuildOutputInPackageOutDir>$(MSBuildProjectDirectory)\bin</TeronisPackagingProjectReferenceBuildOutputInPackageOutDir>-->
    <RestoreSources>$(RestoreSources);$(MSBuildProjectDirectory)\$(TeronisPackagingProjectReferenceBuildOutputInPackageOutDir)</RestoreSources>
  </PropertyGroup>

  <Target Name="BuildDependency" BeforeTargets="CoreBuild">
    <!--<ItemGroup>
      <_DependencyFile Include="$(TeronisPackagingProjectReferenceBuildOutputInPackageOutDir)\*"></_DependencyFile>
    </ItemGroup>-->

    <!--<Delete Files="@(_DependencyFile)" />-->
    <MSBuild Projects="..\src\Teronis.Packaging.ProjectReferenceBuildOutputInPackage.csproj" Targets="pack" Properties="OutDir=$(TeronisPackagingProjectReferenceBuildOutputInPackageOutDir)\"/>
  </Target>

</Project>
